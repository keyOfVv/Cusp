source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'Cusp_Example', :exclusive => true do
  pod 'Cusp', :path => '../'
end

target 'Cusp_Tests', :exclusive => true do
  pod 'Cusp', :path => '../'

  pod 'Quick', '~> 0.8.0'
  pod 'Nimble', '3.0.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
